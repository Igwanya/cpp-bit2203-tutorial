#ifndef BIT2203_HPP
#define BIT2203_HPP

namespace bit2203 {

  /**
   * @brief converting gallons to cubic feet.
   *
   * The user enters the number of gallons and
   * displays equivalent in cubic feet.
   */
  void exe_01();

  /**
   * @brief exe_02
   *
   * Generate a table using std::cout
   */
  void exe_02();

  /**
   * @brief Converting to monetary units.
   *
   * Convert dollars entered by user to other currencies
   * @note:
   *    $1 pound = $1.487US
   *    $1 franc = $0.172US
   *    $1 deutschemark = $0.584 US
   *    $1 yen = $0.00955 US
   */
  void exe_03();

  /**
   *  @brief Convert temperature.
   *
   *  The user enter the temp in degrees.
   *  @note :
   *    f = deg* (9/5) + 32;
   */
  void exe_04();

  /**
   * @brief Sales on commision.
   *
   * A large company pays its salespeople on a commission basis.
   * The salespeople receive Ksh2000 per week plus 9% of their gross
   *  sales for that week.
   */
  void exe_05();

/**
 * @brief Example use of break and continue
 *
 * Break causes the execution to step out of the loop
 *
 *
 *
 */
  void exe_06_break();
  void exe_06_continue();

  /**
   * @brief Calculate odd numbers.
   */
  void exe_07();

  /**
   * @brief Calculate factorial
   */
  void exe_08();

  /**
   * @brief exe_09
   */
  void exe_09();

  /**
   * @brief
   */
  void exe_10();

}

#endif // BIT2203_HPP
